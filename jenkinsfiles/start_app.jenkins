#!groovy
// check run project

pipeline {
    agent any
    stages {
        stage('build image') {
            steps {
                sh 'python3 -m venv venv'
                sh '. venv/bin/activate'
                sh 'python3 -m pip install xyz'
                sh 'pip install -r requirements.txt'
                sh 'docker-compose up -d postgres'
                sh 'docker-compose down'
            }
        }
    }
}